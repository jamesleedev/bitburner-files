/** @type {import('prettier').Config } */
const config = {
  printWidth: 120,
  semi: true,
  singleQuote: true,
  trailingComma: 'es5',
  bracketSpacing: true,
  arrowParens: 'always',
  tabWidth: 2,
  useTabs: false,
};

export default config;
